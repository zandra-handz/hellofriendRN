import { View, Text } from 'react-native'
import React from 'react'

type Props = {}

const useAddToFaves = (props: Props) => {
  return (
    <View>
      <Text>useAddToFaves</Text>
    </View>
  )
}

export default useAddToFaves