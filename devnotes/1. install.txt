in terminal:

npx create-expo-app sampleproject
cd sampleproject
npm start 

install expo app on phone if do not have already
scan QR code to view app on phone


ADDED LATER: to check machine for SSH keys

ls ~/.ssh

to get a key:

cat ~/.ssh/[id_key.pub]